# Direct dependencies, pinned exactly. requirements.lock holds the full
# resolved set with hashes and is what the venv actually installs.
#
# These were unpinned or floored (`ansible`, `ansible-lint>=25.11.0`), so two
# clones a week apart got different toolchains — and the file's own comment
# acknowledged the drift it could not prevent. scripts/check-tool-pins.py now
# fails when a version here disagrees with the matching pre-commit hook.

# ansible-core, not the `ansible` bundle: the bundle ships its own copy of
# community.general, which competed with the pinned one in collections/ and
# made resolution depend on path order.
ansible-core==2.18.3

ansible-lint==26.6.0
pre-commit==4.4.0
yamllint==1.38.0
ruff==0.16.7

# Format-preserving TOML editor for scripts/codex-mcp-sync.py: Codex's
# config.toml is owned by the ChatGPT desktop app, so the sync script must
# round-trip comments/formatting for every section it does not touch.
tomlkit==0.15.1
