ansible
# Keep in step with the ansible-lint rev pinned in .pre-commit-config.yaml.
# When the venv lags behind the hook, `make lint` passes and the commit hook
# then fails on rules the older version did not have.
ansible-lint>=25.11.0
pre-commit>=3.5.0
yamllint>=1.33.0
# Format-preserving TOML editor for scripts/codex-mcp-sync.py: Codex's
# config.toml is owned by the ChatGPT desktop app, so the sync script must
# round-trip comments/formatting for every section it does not touch.
tomlkit>=0.13

# Python linting for the repo's own scripts. Kept in step with the ruff rev
# pinned in .pre-commit-config.yaml, for the same reason as ansible-lint above.
ruff>=0.14.7
