---
description: Orchestrate a broad goal through exploration, bounded delegation, review, correction, and acceptance.
agent: orchestrator
---
Execute broad goal: `$ARGUMENTS`.

Establish acceptance criteria before edits. Inspect repository conventions and current Git status. Build compact dependency-aware ledger with objective, dependencies, assigned agent, expected output, verification, status, and attempts. Route facts to `explorer`, architecture to `architect`, precise mechanics to `worker-fast`, normal coding to `implementer`, unclear failures to `debugger`, known commands to `test-runner`, settled docs to `documentation`, and every meaningful batch to independent `reviewer`.

Parallelize only independent tasks with non-overlapping file scopes and no shared test contention. Keep at most three active independent tasks. Require focused tests, actual diff inspection, reviewer verdict, explicit correction tasks, and final verification. One focused retry to same worker; second similar failure escalates. Do not push, merge, publish, deploy production, or run production load tests.

Return acceptance status, compact ledger, files changed, tests with exit codes, reviewer verdict, unresolved risks, and unsupported checks.
