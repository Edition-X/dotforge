# {{ managed_header }}
# Secret environment variables - source this file to load all secrets.
# Values come from 1Password at apply time (secrets_1password_items in
# group_vars/macbooks.yml); edit them in the 1Password app, then make dotfiles.
export LINEAR_API_KEY="{{ linear_api_key }}"
export NOTION_API_KEY="{{ notion_api_key | default('') }}"
export TODOIST_API_KEY="{{ todoist_api_key }}"
